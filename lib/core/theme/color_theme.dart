import 'package:flutter/material.dart';

class ColorTheme {
  static const primaryColor = Color(0xFF000000);

  static const lightBlueColor = Color(0xFFE2EAFF);
  static const borderColor = Color(0xFF848484);
  static const lightGreyColor = Color(0xFFEBF0FF);

  // //
  static const successColor = Color(0xFF0EB000);
  static const secondary = Color(0xFF002482);
  static const errorColor = Color(0xFFFF5630);

  static const transparentColor = Color(0x00000000);

  // TextColor

  static const darkBlueColor = Color(0xFF223263);
}
